#include <iostream>
using namespace std;

// max를 갱신한 경우 해당 부분은 stack에 남긴다.
// 앞선 값이 더 작으면 stack에서 다 빼준다.

// max는 따로 추가로 더해준다.

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);



	return 0;
}